rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 👇 MODO DESARROLLO - PERMITE TODO
    // Estas reglas permiten lectura y escritura a todos los usuarios autenticados
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

